export interface RegionalFormat {
    uri: string
    id: string
    name: string
    isoCode: string
    localeCode: string
    ui: boolean
    greeting: boolean
    formattingLocale: boolean
    timeFormat: string
    dateFormat: string
}