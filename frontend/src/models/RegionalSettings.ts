export interface RegionalSettings {
    homeCountry: {
        id: string
    }
    timezone: {
        id: string
    }
    language: {
        id: string
    }
    greetingLanguage: {
        id: string
    }
    formattingLocale: {
        id: string
    }
    timeFormat: string
}